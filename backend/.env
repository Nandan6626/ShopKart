# MongoDB connection string
MONGO_URI=

# JWT Secret Key (generate a strong secret for production)
JWT_SECRET=your_strong_jwt_secret_key_here_replace_in_production

# Node Environment
NODE_ENV=development

# Port
PORT=5000
